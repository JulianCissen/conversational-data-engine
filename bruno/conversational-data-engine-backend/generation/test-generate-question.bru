meta {
  name: test-generate-question
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/generation/test
  body: json
  auth: none
}

body:json {
  {
    "type": "ask",
    "field": {
      "id": "transport_type",
      "type": "string",
      "questionTemplate": "What type of transport will you be using?",
      "aiContext": "The user needs to select their mode of transportation. Valid options are 'car' or 'train'. This determines whether we need to collect license plate information.",
      "validation": {
        "type": "string",
        "enum": ["car", "train"],
        "description": "Type of transportation"
      }
    }
  }
}
