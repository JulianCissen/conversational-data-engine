# ==========================================
# LLM Configuration
# ==========================================
# API key for the LLM service
LLM_API_KEY=your-api-key-here

# Base URL for the LLM API endpoint
LLM_BASE_URL=https://api.openai.com/v1

# Model name to use for LLM requests
LLM_MODEL=gpt-4

# Temperature for LLM responses (0-2, default: 0)
# Higher values make output more random, lower values more deterministic
LLM_TEMPERATURE=0

# ==========================================
# Database Configuration
# ==========================================
# Database host (use 'postgres' for Docker, 'localhost' for local development)
# Default: localhost
DB_HOST=localhost

# Database port
# Default: 5432
DB_PORT=5432

# Database name
# Default: form_engine
DB_NAME=form_engine

# Database user
# Default: admin
DB_USER=admin

# Database password
# Default: password123
DB_PASSWORD=password123

# ==========================================
# Language Configuration
# ==========================================
# Default language mode for conversations (adaptive or strict)
# - adaptive: AI adapts to the user's language automatically
# - strict: Enforces communication in a specific language only
# Default: adaptive
LANG_DEFAULT_MODE=adaptive

# Default language code (ISO-639 format, e.g., en-GB, de-DE, fr-FR)
# This is used as the default/fallback language for all conversations
# Default: en-GB
LANG_DEFAULT_LANGUAGE=en-GB

# ==========================================
# Plugin Configuration
# ==========================================
# Absolute path to directory where plugins are located (default: <backend>/plugins)
PLUGINS_DIRECTORY=/absolute/path/to/plugins

# ==========================================
# Server Configuration
# ==========================================
# Port for the backend server (default: 3100)
# PORT=3100

# CORS origin for frontend communication
# Default: http://localhost:3000
CORS_ORIGIN=http://localhost:3000
